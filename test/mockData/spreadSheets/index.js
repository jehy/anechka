'use strict';

const timetable = require('./timetable');
const users = require('./users');
const usersExpected = require('./users.expected');
const timetableExpected = require('./timetable.expected');

module.exports = {
  users,
  timetable,
  usersExpected,
  timetableExpected,
};
